# Handoff Slice 1.1 - testes de validação do contrato LLM1

## Contexto

Change: `room2-report-origin-recent-exams-transfusion`.

Objetivo da fase: preparar TDD para novos campos estruturados de origem, transfusão e exames rastreados com recência no LLM1.

## Objetivo do slice

Adicionar testes em `tests/unit/test_llm1_validation.py` que inicialmente falhem (red) para os novos campos esperados no contrato.

## Escopo permitido

- Alterar apenas:
  - `tests/unit/test_llm1_validation.py`
- Não implementar schema ainda.

## Critérios de aceite

- Há cenários cobrindo:
  - origem (cidade/hospital/unidade/UF opcional);
  - transfusão com resposta binária (`yes`/`no`) e unidades inteiras;
  - exames rastreados com marcação de item mais recente.
- Testes novos falham por ausência de implementação (red).

## Comandos

```bash
uv run pytest tests/unit/test_llm1_validation.py -q
uv run ruff check tests/unit/test_llm1_validation.py
uv run mypy tests/unit/test_llm1_validation.py
```

## Prompt de execução (cole no glm-5.1)

```text
Use o skill openspec-apply-change.
Change ID: room2-report-origin-recent-exams-transfusion
Task file: openspec/changes/room2-report-origin-recent-exams-transfusion/tasks.md
Implemente somente o slice 1.1 (testes TDD do contrato LLM1).
Regras:
- TDD: escreva testes primeiro e valide estado RED.
- Não altere arquivos fora do escopo permitido.
- Mantenha type hints/docstrings quando aplicável.
- Rode os comandos de verificação do slice.
- Atualize o checklist do tasks.md marcando apenas 1.1.
- Gere relatório detalhado em /tmp/openspec-slice-report-1.1.md com:
  - resumo do objetivo
  - arquivos alterados
  - comandos executados + status
  - evidência de RED
  - diff snippets
- Commit e push com mensagem relacionada ao slice.
- Pare e aguarde aprovação.
```

## Prompt de validação

```text
Valide se o slice 1.1 está correto:
1) confira que só tests/unit/test_llm1_validation.py foi alterado;
2) confira que os novos testes cobrem origem, transfusão binária e recência de exames;
3) confira evidência de falha RED;
4) confira tasks.md com 1.1 marcado e nenhum outro item.
Registre resultado em /tmp/openspec-slice-report-1.1.md.
```

## Prompt de atualização do tasks.md

```text
Atualize openspec/changes/room2-report-origin-recent-exams-transfusion/tasks.md:
- marque [x] somente o item 1.1;
- não altere os demais itens.
```
