# Handoff Slice 1.2 - implementação do schema LLM1

## Contexto

Continuação da fase 1 após 1.1 aprovado.

## Objetivo do slice

Implementar em `src/triage_automation/application/dto/llm1_models.py` os novos modelos/campos para origem, transfusão e exames rastreados, fazendo os testes de 1.1 passarem (green).

## Escopo permitido

- Alterar:
  - `src/triage_automation/application/dto/llm1_models.py`
  - `tests/unit/test_llm1_validation.py` (apenas ajustes mínimos se necessários)

## Regras funcionais obrigatórias

- Transfusão sem estado indeterminado (binário `yes|no`).
- Unidades de transfusão em inteiro quando houver.
- Compatibilidade com `extra=forbid`.

## Critérios de aceite

- Testes do slice 1.1 passam.
- Validação de schema rejeita payload inválido para transfusão e campos extras.

## Comandos

```bash
uv run pytest tests/unit/test_llm1_validation.py -q
uv run ruff check src/triage_automation/application/dto/llm1_models.py tests/unit/test_llm1_validation.py
uv run mypy src/triage_automation/application/dto/llm1_models.py tests/unit/test_llm1_validation.py
```

## Prompt de execução (cole no glm-5.1)

```text
Use o skill openspec-apply-change.
Change ID: room2-report-origin-recent-exams-transfusion
Task file: openspec/changes/room2-report-origin-recent-exams-transfusion/tasks.md
Implemente somente o slice 1.2 (schema LLM1).
Regras:
- Consuma os testes já criados no slice 1.1 e faça GREEN.
- Transfusão deve ser estritamente binária (yes/no), sem unknown.
- Mantenha mudanças mínimas e reversíveis.
- Rode comandos de verificação.
- Atualize tasks.md marcando somente 1.2.
- Gere relatório em /tmp/openspec-slice-report-1.2.md (objetivo, arquivos, comandos, evidência green, snippets).
- Commit e push.
- Pare e aguarde aprovação.
```

## Prompt de validação

```text
Valide o slice 1.2:
- checar implementação do novo contrato em llm1_models.py;
- confirmar binário yes/no em transfusão;
- confirmar testes unitários green;
- confirmar tasks.md com 1.2 marcado.
Registrar em /tmp/openspec-slice-report-1.2.md.
```

## Prompt de atualização do tasks.md

```text
Marque apenas [x] 1.2 em openspec/changes/room2-report-origin-recent-exams-transfusion/tasks.md.
```
