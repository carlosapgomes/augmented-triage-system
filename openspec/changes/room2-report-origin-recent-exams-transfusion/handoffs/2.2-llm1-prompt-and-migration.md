# Handoff Slice 2.2 - implementação de prompt LLM1 + migração v6

## Contexto

Continuação da fase 2 após 2.1 aprovado.

## Objetivo do slice

- Atualizar instruções de prompt em `src/triage_automation/application/services/llm1_service.py`.
- Criar migração Alembic para versionar `llm1_system` e `llm1_user` v6 com conteúdo alinhado.

## Escopo permitido

- Alterar:
  - `src/triage_automation/application/services/llm1_service.py`
  - `alembic/versions/<nova_migracao>.py`
  - testes afetados de prompt (`tests/integration/test_llm_prompt_loading_runtime.py`)

## Regras funcionais obrigatórias

- Prompt deve instruir explicitamente:
  - origem (cidade/hospital/unidade/UF opcional);
  - recência por data/hora com fallback por posição textual e desempate pela última ocorrência;
  - transfusão binária e ausência como `não`.

## Critérios de aceite

- Testes do slice 2.1 passam.
- Migração ativa v6 e desativa versão anterior para os prompts LLM1.

## Comandos

```bash
uv run pytest tests/integration/test_llm_prompt_loading_runtime.py -q
uv run ruff check src/triage_automation/application/services/llm1_service.py alembic/versions
uv run mypy src/triage_automation/application/services/llm1_service.py
```

## Prompt de execução (cole no glm-5.1)

```text
Use o skill openspec-apply-change.
Change ID: room2-report-origin-recent-exams-transfusion
Task file: openspec/changes/room2-report-origin-recent-exams-transfusion/tasks.md
Implemente somente o slice 2.2 (prompt LLM1 e migração v6).
- Faça GREEN dos testes criados no slice 2.1.
- Crie migração Alembic versionando llm1_system/llm1_user v6.
- Mantenha o estilo das migrações já existentes no repositório.
- Rode os comandos de verificação.
- Atualize tasks.md marcando somente 2.2.
- Gere relatório em /tmp/openspec-slice-report-2.2.md.
- Commit e push.
- Pare e aguarde aprovação.
```

## Prompt de validação

```text
Valide o slice 2.2:
- llm1_service.py contém instruções novas de origem/recência/transfusão;
- migração Alembic v6 criada e coerente;
- testes de prompt em GREEN;
- tasks.md com apenas 2.2 marcado.
Registrar em /tmp/openspec-slice-report-2.2.md.
```

## Prompt de atualização do tasks.md

```text
Marque apenas [x] 2.2 em openspec/changes/room2-report-origin-recent-exams-transfusion/tasks.md.
```
